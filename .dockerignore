# Slim build context (Railway / docker build). Keep in sync with local-only trees in .gitignore.
# Never bake a developer's laptop-only canary.yaml; Dockerfile copies canary.example.yaml instead.
configs/entities/canary.yaml
bumbleweb/
# Bumble AR: keep out of slim Docker build context (spatial UI is developed/built locally or in CI).
bumbleAR/
bee-model/
onboarding/
docs/

.git
.gitignore
.dockerignore
**/__pycache__
**/*.py[cod]
**/.pytest_cache
.venv
venv
node_modules
*.egg-info
.eggs
dist
htmlcov
.coverage
.mypy_cache
.ruff_cache
