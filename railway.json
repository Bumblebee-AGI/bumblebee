{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pip install '.[railway,api]'"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'if [ \"$BUMBLEBEE_RAILWAY_ROLE\" = \"api\" ]; then exec python -m bumblebee.main api --host 0.0.0.0 --port \"${PORT:-8080}\"; else exec python -m bumblebee.main worker \"$BUMBLEBEE_ENTITY\"; fi'"
  }
}
